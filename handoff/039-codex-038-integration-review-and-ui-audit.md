# 039 ｜ Codex → Claude ｜ 038 独立集成复核、研究冻结定稿与真实页面验收
- 日期：2026-08-18（Asia/Shanghai）
- 集成基线：`develop-v1@12ea8279c4ea33f804574d2060173ecb22b3f6e1`
- 文件租约：Codex 仅修改 `docs/ACT_MODEL_DRAFT.md`、本回执与 `handoff/README.md`；未修改 `frontend/src/**`
- 需要对方行动：是（继续既有 028 B2；同时纳入 §4 的即时统计缺陷）

## 1. Claude 038 独立复核结论

`WordbooksView.test.ts` 的三条回归分别覆盖导入成功报告、失败错误态和真实空态文案；实现 diff 与现有 API 合同一致。Codex 独立执行结果：

- 后端：`140 passed, 14 skipped`；
- 前端：`56 passed`；
- `vue-tsc` 与 Vite 生产构建：通过；
- `git diff --check 6cb47d8..12ea827`：通过。

`FEATURE_STATUS` 共有 397 行，阶段归属 397/397 非空，状态仍为 `✅28 / 🟡100 / ⬜269`。显式检查点编号与 033/035 逐项完全一致，但数量是 **77 条**，不是 038 和提交说明中的 78 条；分域为 LIB10、REA6、VOC20、REV10、PLN5、ANA4、DAT9、EXM11、CUR2。阶段列内容正确，本回执纠正计数口径，旧回执保持不可变。

## 2. ACT 研究冻结正式生效

接受 038 的双签与两条实现期注记，并把 `ACT_MODEL_DRAFT.md` 状态由“冻结候选”改为“研究冻结”：

1. `renderer_contract_json.conflict_policy` 使用受控枚举；409 必须返回服务器 revision、可恢复草稿和允许动作，禁止静默 last-write-wins。
2. 拼写/填空等有界低敏感短答案默认可在 `response_events` 保存完整值，但必须有长度上限、敏感字段排除和本地隐私设置；作文/录音仍只保留版本、hash 或资产引用。

这只冻结边界，不授权建表、迁移或 API 变更。任何实现仍按“提案 → 用户确认 → DATA_MODEL/API_CONTRACT → 编号迁移”执行。

研究冻结四项材料至此正式齐备；**这不表示 2026-08-30 早期可用检查点已经完成，也不构成停工点**。

## 3. 真实页面通过项

Codex 在隔离临时数据库和本地开发服务上按真实用户路径验收：

- 激活 CET4 内置词书后生成 20 张新卡，首张必为教学卡；
- 给首词评分后进入下一词，刷新页面后进度持久化为 `1/20`；
- 导入 `test-fixtures/sample-article.txt` 后可进入六段阅读器；
- 整篇完型提交得到 `2/10`、16 道错题，错题本正确聚合为 2026 年 1 篇 16 题。

## 4. 当前早期检查点阻塞项

1. **B2 / REV-01/07/18**：`/review` 无路由，页面仅剩空白 `<main>`，控制台明确报 `No match found for location with path "/review"`；主导航也没有统一复习中心入口。
2. **B4/B5 / PLN-02/03/04 + ANA-03**：首页仍是旧刷题概览，没有今日任务与日报入口或用户可见数据。
3. **B6 / DAT-07~11**：设置页只有模型/API 工作区，没有备份、校验、预演和恢复 UI。
4. **词汇即时统计**：单词评分后卡片进度从 `0/20` 变为 `1/20`，但顶部“今日新学”仍显示 `0/20`；整页刷新后才变为 `1/20`。请在 B2 批次中补即时刷新与组件回归。

Claude 继续 028 的既有顺序：B2 → B4/B5 → B6 → A11/A12。Codex 不接管 `frontend/src/**`，等待每批可验证提交后独立审查、真实页面复测和全量回归；等待期间继续做不重叠的集成与验收工作。

—— Codex
