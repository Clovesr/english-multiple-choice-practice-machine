# V1 发布审计（持续更新）

> 本文件是发布证据台账，不修改 `V1_SCOPE.md` / `ACCEPTANCE.md` 的范围与判定口径。
> 任何“通过”都必须能追溯到当前提交上的自动测试、真实生产构建或人工验收；没有证据的项目保持未完成。

## 当前审计基线

- 审计日期：2026-08-18（Asia/Shanghai）
- 主线提交：`develop-v1@415526c`
- 发布闸门：`tools/v1_release_preflight.ps1`
- 后端全量：`141 passed, 13 skipped`（13 项为未提供私有全题库目录时的环境跳过）
- 前端全量：`53 passed`
- 类型与构建：`vue-tsc --noEmit`、Vite production build 通过，`dist/index.html` 存在
- 依赖与静态门槛：`pip check`、`compileall`、`git diff --check` 通过
- 风险台账：`KNOWN_ISSUES.md` 未修项中 P0/P1 为 0
- 尚存测试噪音：`StudyCardView.test.ts` 未注册 `RouterLink`，属于 handoff 028 的前端收尾项，不能作为最终无警告证据

## 新装与重启生产冒烟

在中文路径下从 `415526c` 创建隔离 detached worktree，重新安装前端依赖并构建 production 产物，使用项目虚拟环境启动真实 FastAPI 静态托管服务。隔离目录与独立数据库在验收后已删除，未读取或修改主工作树的 `backend/data/`。

### 首次启动

| 证据 | 结果 |
|---|---|
| `/api/health` | 200，`status=ok` |
| `/` | 200，HTML 含 `window.__LINJIAN_STARTUP__` 注入 |
| production JS 资产 | 200 |
| `/api/startup` | 200；活动题库 17 卷、102 篇、765 题 |
| `/api/resources` | 200；空结果结构完整（`items=[]`, `total=0`） |
| `/api/wordbooks` | 200；离线自动安装 CET4/CET6/考研三本词书 |
| `/api/study/overview` | 200；空学习历史返回 7 日预测与默认计划 |
| `/api/review/queue` | 200；空队列结构完整 |
| `/api/reports/daily` | 200；空日报结构完整 |
| 数据库 | 自动创建；迁移版本 0001–0006 全部登记；WAL 生效 |

内置词书首启词量分别为 CET4 `3849`、CET6 `5407`、考研 `4801`。

### 完全退出后重启

服务正常关闭后，以同一隔离数据目录重新启动。健康检查继续通过，活动题库仍为 `17/102/765`，资源仍为 0，三本词书 ID 与词量不变，未出现重复安装或重复迁移。

这证明 B7 的“空数据目录首启正常”和基础重启幂等在生产托管层面成立；各页面的空态文案与下一步引导仍需最终可视验收，不能仅凭接口 200 判定 G2 全部完成。

## ACCEPTANCE 覆盖矩阵

状态定义：

- **自动通过**：当前主线有直接自动化覆盖，且本轮全量执行通过。
- **生产冒烟通过**：当前 production build 上实测通过，但不替代完整人工交互。
- **部分完成**：后端或局部链路有证据，完整场景仍缺前端/人工证据。
- **未完成**：当前实现明确缺少验收所需能力。
- **需时间证据**：实现不能替代规定的观察周期。

| 场景 | 当前状态 | 直接证据 / 剩余缺口 |
|---|---|---|
| G1 重启保持 | 部分完成 | 资源、迁移、学习、备份均有自动化；本轮新装生产重启计数保持。最终 A7/B6 用户链路仍需真实交互复验。 |
| G2 空数据 | 部分完成 | production 首启与空 API 通过；所有新页面的空态引导尚未逐页可视验收。 |
| G3 错误路径 | 部分完成 | 坏资源、损坏备份、非法 API 等自动化通过；新增前端表单的输入保留与可读错误仍待 028。 |
| G4 回归 | 自动通过 | 发布闸门：后端 141/13、前端 53、类型检查与构建通过。 |
| G5 中文环境 | 部分完成 | 中文工作树生产启动通过；GBK/中文资源自动化通过；最终 UI 文件选择与显示需复验。 |
| A1–A4 资源/阅读/收藏 | 部分完成 | `test_resources.py`、`test_vocabulary_learning.py` 覆盖后端；既有真实 UI 曾验收，需在最终集成提交重跑 A7。 |
| A5–A7 学习与 FSRS | 部分完成 | `test_study.py`、`test_migrations.py` 覆盖词级 FSRS、幂等、暂停和重启；A7 真实文章全链路需最终人工执行。 |
| A8 词书与词典 | 部分完成 | 离线词书首启和词典自动化通过；自定义词表导入 UI 未完成（handoff 028）。 |
| A9–A10 六题型/判分/发音 | 部分完成 | 六题型共用词级 FSRS、客观题复判与语音降级均有自动化；最终 production 交互仍需逐题型验收。 |
| A11 每日计划/积压/冲刺 | 部分完成 | 后端 round-trip 与行为测试通过；积压三模式及冲刺的完整 UI 未完成（handoff 028）。 |
| A12 顽固卡 | 部分完成 | 后端 leech 计数、暂停语义通过；卡片级建议与操作 UI 未完成（handoff 028）。 |
| B1 PDF/DOCX 资源导入 | 部分完成 | `test_resources.py` 覆盖解析与扫描 PDF 回退；资源库上传 UI 仍只开放 TXT/Markdown（handoff 028）。 |
| B2 错题复习中心 | 部分完成 | `test_review.py` 覆盖创建、去重、评分幂等和暂停；`/review` 页面未完成（handoff 028）。 |
| B3 知识点与掌握度 | 自动通过 | `test_skills.py` 覆盖 5 题证据与删除课程不级联。 |
| B4 今日任务 | 部分完成 | `test_tasks_reports.py` 覆盖幂等、顺延、完成；首页 UI 未完成（handoff 028）。 |
| B5 日报 | 部分完成 | `test_tasks_reports.py` 覆盖事件对账与重算；首页日报 UI 未完成（handoff 028）。 |
| B6 备份恢复 | 部分完成 | `test_backups.py` 覆盖创建、校验、空环境恢复、损坏拒绝、强杀回滚；设置页 UI 未完成（handoff 028）。 |
| B7 发布闸门 | 未完成 | 自动闸门和新装首启已通过；W1/W2 前端缺口未闭环，且“真实使用 ≥3 天无数据丢失”尚无时间证据。 |

## 当前发布阻断项

以下项目仍是 V1 原范围，不得降级或从两周计划中删除：

1. 资源库 PDF/DOCX 上传入口与失败回退展示。
2. 自定义词表导入、匹配报告和词典外词提示。
3. `/review` 统一错题复习中心。
4. 首页今日任务与日报。
5. 设置页备份创建、校验、dry-run 预览和二次确认恢复。
6. 词汇积压三模式、考试冲刺及顽固卡卡片级建议。
7. `StudyView.vue` SFC 样式块修正、`RouterLink` 测试警告清零以及新增页面加载/空/错/危险确认测试。
8. 上述增量合并后的 production build 逐场景复验，以及 ≥3 天真实使用证据。

这些前端项沿用既有 `claude/4-study-ui` 专线和 `frontend/src/` 租约；协调任务负责独立 diff 审查、主线集成、全量发布闸门和最终验收。
